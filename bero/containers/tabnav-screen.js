import React from 'react';
import { View } from 'react-native';
import { TabNav } from '../navigation/router';

export default class HeroScreen extends React.Component {
  render() {
    return (
      <TabNav />
    );
  }
}